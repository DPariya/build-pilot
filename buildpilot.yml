name: my-app

build:
  image: node:18
  steps:
    - cd backend && npm install
    - cd backend && npm test
    - cd backend && npm run build

deploy:
  target: production
  environment:
    NODE_ENV: production
    PORT: 3000
